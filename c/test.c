#include <stdio.h>
int main() {

  // char sentence[] = "   This   is a random string  ";
  // printf("\nString: ");
  // puts(sentence);
  // int stringLength = sizeof(sentence) - 1;
  // int count = 0;
  // for (int i = 0; sentence[i] != '\0'; i++) {
  //   if ((sentence[i] == ' ' && sentence[i + 1] != ' ' &&
  //        sentence[i + 1] != '\0')) {
  //     printf("Checking: %d", i);
  //     count++;
  //   }
  // }
  // printf("\nNumber of words = %d", count);

  // int arr[] = {2, 3, 4};
  // int number = 0;
  // int length = sizeof(arr) / sizeof(arr[0]);
  // for (int i = 0; i < length; i++) {
  //   number = (number * 10) + arr[i];
  // }
  // number *= 2;
  // printf("%d", number);
  return 0;
}
